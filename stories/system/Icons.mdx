import { Meta, IconGallery, IconItem } from "@storybook/addon-docs/blocks";
import * as Icons from "../../src/shared/ui/components/icon";

<Meta title="System/Icons" />

<IconGallery>
	{Object.entries(Icons).map(([name, Icon], ind) => (
		<IconItem name={name} key={ind}>
			<Icon />
		</IconItem>
	))}
</IconGallery>
